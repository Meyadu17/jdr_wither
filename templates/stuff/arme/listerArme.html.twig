{% extends 'base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}

    <div class="titre-logo">
        <img class="logo" src="{{ asset('images/logo-armes.jpg') }}" alt="Logo" />
        <h1>Les armes</h1>
        <img class="logo" src="{{ asset('images/logo-armes.jpg') }}" alt="Logo" />
    </div>

    <section class="admin_body_container" >
        <a class="btn btn-outline-info" href="{{ path('app_admin_arme_ajouter') }}">
            Mettre une arme en vente
        </a>
        <a class="btn btn-outline-light" href="{{ path('app_stuff_lister') }}">Retour</a>
    </section>
    <br>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success" role="alert">
            {{ message }}
        </div>
    {% endfor %}

    <section class="admin_body_container">
        <div class="accordion" id="accordionPanelsStayOpenExample">
            {% for typeArme in typeArmes %}
                <div class="accordion-item accordion-color">
                    <h2 class="accordion-header">
                        <button class="accordion-button collapsed" type="button"
                                data-bs-toggle="collapse" data-bs-target="#panelsStayOpen-collapseThree"
                                aria-expanded="false" aria-controls="panelsStayOpen-collapseThree">
                            {{typeArme.libelle}}
                        </button>
                    </h2>
                    <div id="{{ 'collapse_' ~ typeArme.id }}" class="accordion-collapse collapse show">
                        <div class="accordion-body">
                            <table class="table table-dark">
                                <thead>
                                <tr>
                                    <th scope="col">Nom</th>
                                    <th scope="col">Dégats</th>
                                    <th scope="col">Mains</th>
                                    <th scope="col">Portée</th>
                                    <th scope="col">Effets</th>
                                    <th scope="col">Poids</th>
                                    <th scope="col">Taille</th>
                                    <th scope="col">Prix</th>
                                    <th scope="col">Action</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for arme in typeArme.armes %}
                                    <tr>
                                        <td>{{arme.nom}}</td>
                                        <td>{{arme.degat}}</td>
                                        <td>{{arme.mains}}</td>
                                        <td>{{arme.portee}}</td>
                                        <td>{{arme.effet}}</td>
                                        <td>{{arme.poids}}</td>
                                        <td>{{arme.taille.libelle}}</td>
                                        <td>{{arme.prix}}</td>
                                        <td>
                                            <a class="btn btn-outline-info" href="{{ path('app_admin_arme_modifier', { 'id' : arme.id }) }}">
                                                modifier
                                            </a>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}
