{% extends 'base.html.twig' %}

{% block title %}Races{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}

{% block body %}

    <div class="btn-group" role="group" aria-label="Default button group">
        <a class="btn btn-outline-info" href="{{ path('app_guide_personnage_talents') }}">Talents</a>
        <a class="btn btn-outline-info" href="{{ path('app_guide_personnage_handicaps') }}">Handicaps</a>
        <a class="btn btn-outline-info" href="{{ path('app_guide_personnage_jobs') }}">Jobs</a>
        <a class="btn btn-info" href="{{ path('app_guide_personnage_races') }}">Races</a>
        <a class="btn btn-outline-light" href="{{ path('app_guide_personnage_lister') }}">Retour</a>
        {% if is_granted('ROLE_ADMIN') %}
            <a class="btn btn-outline-warning" href="#">
                Nouveau
            </a>
        {% endif %}
    </div>

    <h1>Races</h1>

    <div class="card-element">
         <a href="#">
            <div class="card card-size fond-image">
                <img src="{{ asset('images/competencesCombat/don.jpg') }}" class="card-img-top" alt="magasin">
                <div class="card-body">
                    <h5 class="card-title">Races</h5>
                    <p class="card-text">Duis aute irure dolor in reprehenderit in voluptate velit.</p>
                </div>
            </div>
        </a>
    </div>
{% endblock %}
