{% extends 'base.html.twig' %}

{% block title %}Races{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('formControl') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('formControl') }}
{% endblock %}

{% block body %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger" role="alert">
            {{ message }}
        </div>
    {% endfor %}

    <section class=" text-center text-lg-start">

        <h1>Nouvelle race</h1>
        <div class="card mb-3">
            <div class="row g-0 d-flex align-items-center fond-image">

                <div class="col-lg-4 d-none d-lg-flex">
                    <img src="{{ asset('images/guideCreation/races.jpg') }}" alt="Trendy Pants and Shoes"
                         class="w-100 rounded-t-5 rounded-tr-lg-0 rounded-bl-lg-5" />
                </div>
                <div class="col-lg-8">
                    <div class="card-body py-5 px-md-5">

                        {{ form_start(form, {'attr':{'class':'row g-3 controle-form', 'novalidate': 'novalidate'}}) }}
                        <!-- Ligne 1 -->
                        <div class="row">
                            <div class="form-outline">
                                {{ form_widget(form.libelle) }}
                                {{ form_label(form.libelle) }}
                            </div>
                            <div class="col-md-6 mb-4">
                                <div class="form-outline">
                                    {{ form_widget(form.description) }}
                                    {{ form_label(form.description) }}
                                </div>
                            </div>
                        </div>

                        <!-- Ligne 2 -->
                        <div class="row">
                            <div class="col-md-12 mb-4">
                                <div class="form-outline">
                                    {{ form_widget(form.photo) }}
                                    {{ form_label(form.photo) }}
                                </div>
                            </div>
                        </div>

                        <!-- Submit button -->
                        <div class="col-md-6 mb-4">
                            {% if app.request.get('_route') == "app_admin_race_ajouter" %}
                                <button class="btn btn-outline-info" type="submit">Ajouter</button>
                                <button class="btn btn-outline-info" type="submit" name="create_and_new">Valider et créer de nouveau</button>
                            {% else %}
                                <button class="btn btn-outline-info" type="submit">Modifier</button>
                            {% endif %}
                            <a href="{{ path('app_guide_personnage_races') }}" class="btn btn-outline-light">Retour</a>
                        </div>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
